# Desenvolvimento Local:
# Não precisa definir VITE_API_URL - o Vite faz proxy automático de /api para http://localhost:8000
# O proxy está configurado em vite.config.ts

# Produção (Railway):
# VITE_API_URL=https://backend-nitrosubs.up.railway.app

# Use true apenas para desenvolver sem backend (dados mock)
VITE_USE_MOCK=false
