# URL do backend (opcional: em dev o Vite faz proxy de /api para http://localhost:8000)
# VITE_API_URL=http://localhost:8000

# Use true apenas para desenvolver sem backend (dados mock)
VITE_USE_MOCK=false
